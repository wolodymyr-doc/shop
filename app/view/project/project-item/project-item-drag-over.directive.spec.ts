import { ProjectItemDragOverDirective } from './project-item-drag-over.directive';

describe('ProjectItemDragOverDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectItemDragOverDirective();
    expect(directive).toBeTruthy();
  });
});
